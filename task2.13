SELECT pr.project_id
FROM task2.supplies s
JOIN task2.providers pro
ON pro.provider_id = s.provider_id
JOIN task2.projects pr
ON pr.project_id = s.project_id
WHERE pro.city <> ANY (SELECT city
		      FROM task2.projects)
		      

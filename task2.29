SELECT s.project_id
FROM task2.supplies s 
JOIN task2.projects pro
ON s.project_id = pro.project_id
WHERE provider_id = 'П1'
EXCEPT (SELECT project_id
	FROM task2.supplies
	WHERE provider_id != 'П1'
	)
	
